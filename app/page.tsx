"use client";
import {useState} from "react";
import {Activity,Eye,EyeOff,Lock,Mail,ShieldCheck} from "lucide-react";
const roles=[['ADMIN','Administrator'],['OPERATOR','Operator'],['SUPERVISOR','Supervisor'],['DEPARTMENT_OFFICER','Department Officer']] as const;
export default function Login(){const [role,setRole]=useState<typeof roles[number][0]>('OPERATOR'),[show,setShow]=useState(false),[error,setError]=useState(''),[busy,setBusy]=useState(false);async function submit(f:FormData){setBusy(true);setError('');const r=await fetch('/api/auth/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({email:f.get('email'),password:f.get('password'),role})});const b=await r.json();setBusy(false);if(!r.ok){setError(b.error);return;}location.href=`/${b.role==='DEPARTMENT_OFFICER'?'department':b.role.toLowerCase()}/dashboard`;}return <main className="login"><section className="login-brand"><div className="login-mark"><Activity/>9141</div><p className="login-kicker">ADAMA ADMINISTRATION</p><h1>Events escalation.<br/><em>Responses that matter.</em></h1><p>One secure workspace for citizen calls, emergency routing and accountable service delivery.</p><div className="workflow"><span>Citizen call</span><i/> <span>Operator intake</span><i/> <span>Department response</span><i/> <span>Resolution</span></div></section><section className="login-panel"><form action={submit}><div className="mobile-mark"><Activity/> 9141</div><p className="eyebrow">SECURE ACCESS</p><h2>Welcome back</h2><p className="muted">Sign in to the Call Center 9141 system.</p><label>Access role<div className="role-grid">{roles.map(r=><button type="button" className={role===r[0]?'selected':''} onClick={()=>setRole(r[0])} key={r[0]}><ShieldCheck/>{r[1]}</button>)}</div></label><label>Email address<div className="field"><Mail/><input required type="email" name="email" defaultValue="demo@9141.local" placeholder="name@organization.gov.et"/></div></label><label>Password<div className="field"><Lock/><input required name="password" type={show?'text':'password'} defaultValue="Demo@9141" placeholder="Enter your password"/><button type="button" onClick={()=>setShow(!show)}>{show?<EyeOff/>:<Eye/>}</button></div></label>{error&&<div className="login-error">{error}</div>}<button className="login-submit" disabled={busy}>{busy?'Signing in...':'Sign in securely'}</button><p className="hint">Demo credentials are prefilled. Select any role to view its workspace.</p></form></section></main>}
